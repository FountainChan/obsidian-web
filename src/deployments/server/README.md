# obsidian-web — server deployment (Node.js / Docker)

פריסת-שרת (Docker או node רגיל); הקוד ב-`src/runtime-server/server`.
HTTPS+auth ע"י reverse-proxy חיצוני (Caddy מומלץ).

ראה `README.md` (שורש הריפו), סעיף "Node.js deployment", לפרטי setup מלאים.
